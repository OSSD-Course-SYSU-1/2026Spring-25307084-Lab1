# 餐食打卡功能说明

## 功能概述

在打卡界面加入了上传图片与文字功能，使用户能记录每餐吃了什么。

## 主要功能

### 1. 图片上传
- **拍照功能**：点击"📷 拍照"按钮，调用相机拍摄食物照片
- **相册选择**：点击"🖼️ 相册"按钮，从相册选择食物图片
- **图片预览**：选择后显示图片预览，可删除重新选择
- **图片展示**：打卡记录中显示食物图片缩略图

### 2. 文字记录
- **食物描述输入**：在 TextArea 输入框中记录食物内容
- **提示文字**："记录今天吃了什么..."
- **多行支持**：支持输入多行文字描述
- **文字展示**：打卡记录中显示食物描述（最多3行）

### 3. 打卡流程
```
1. 点击餐次卡片（早餐/午餐/晚餐）
2. 弹出打卡界面
3. 选择图片（可选）
   - 拍照
   - 从相册选择
4. 输入食物描述（可选）
5. 选择健康等级
   - 健康（绿色）
   - 中等（黄色）
   - 不健康（红色）
6. 点击确认完成打卡
```

### 4. 记录展示
打卡记录卡片包含：
- 餐次图标和时间范围
- 打卡状态和健康等级
- 打卡时间
- **食物图片**（如有）
- **食物描述**（如有）

### 5. 数据存储
- 使用 StorageUtil 本地持久化存储
- 按日期保存打卡记录
- 支持查看历史记录
- 月视图统计功能

## 技术实现

### 核心组件
- `MealCheckInPage.ets`：打卡页面主组件
- `StorageUtil.ets`：本地存储工具

### 关键代码

#### 图片选择
```typescript
// 从相册选择图片
private async selectImage(): Promise<void> {
  const photoSelectOptions = new picker.PhotoSelectOptions()
  photoSelectOptions.MIMEType = picker.PhotoViewMIMETypes.IMAGE_TYPE
  photoSelectOptions.maxSelectNumber = 1
  
  const photoPicker = new picker.PhotoViewPicker()
  const photoSelectResult = await photoPicker.select(photoSelectOptions)
  
  if (photoSelectResult.photoUris.length > 0) {
    this.foodImage = photoSelectResult.photoUris[0]
  }
}
```

#### 文字输入
```typescript
TextArea({ placeholder: '记录今天吃了什么...', text: this.foodText })
  .width('100%')
  .height(80)
  .onChange((value: string) => {
    this.foodText = value
  })
```

#### 数据保存
```typescript
interface MealRecord {
  meal: string        // breakfast, lunch, dinner
  healthLevel: HealthLevel
  time: string        // 打卡时间
  foodImage?: string  // 食物图片路径
  foodText?: string   // 食物描述文字
}
```

## 使用示例

### 示例1：早餐打卡
1. 点击"🌅 早餐"卡片
2. 点击"拍照"拍摄早餐照片
3. 输入："吃了燕麦粥、鸡蛋和牛奶"
4. 选择"健康"
5. 点击确认

### 示例2：午餐打卡
1. 点击"☀️ 午餐"卡片
2. 点击"相册"选择午餐照片
3. 输入："红烧肉、炒青菜、米饭"
4. 选择"中等"
5. 点击确认

### 示例3：晚餐打卡
1. 点击"🌙 晚餐"卡片
2. 不上传图片
3. 输入："沙拉和全麦面包"
4. 选择"健康"
5. 点击确认

## 界面说明

### 打卡弹窗
```
┌─────────────────────────┐
│  🌅 早餐打卡             │
├─────────────────────────┤
│  ┌─────┐  ┌─────┐       │
│  │ 📷  │  │ 🖼️  │       │
│  │拍照 │  │相册 │       │
│  └─────┘  └─────┘       │
├─────────────────────────┤
│  食物描述                │
│  ┌───────────────────┐  │
│  │记录今天吃了什么... │  │
│  └───────────────────┘  │
├─────────────────────────┤
│  健康评估                │
│  🟢健康  🟡中等  🔴不健康│
├─────────────────────────┤
│      [确认]  [取消]      │
└─────────────────────────┘
```

### 打卡记录卡片
```
┌─────────────────────────┐
│  🌅 早餐  6:00 - 9:00    │
│  ✓ 已打卡  健康          │
│  打卡时间: 08:30         │
│  ┌───────────────────┐  │
│  │   [食物图片]       │  │
│  └───────────────────┘  │
│  吃了燕麦粥、鸡蛋和牛奶  │
└─────────────────────────┘
```

## 编译状态
✅ 编译成功，无错误

## 注意事项
1. 图片和文字都是可选的，可以只记录其中一项
2. 图片存储为本地路径，不会占用过多存储空间
3. 历史记录可通过月视图查看
4. 数据存储在本地，清除应用数据会丢失记录
